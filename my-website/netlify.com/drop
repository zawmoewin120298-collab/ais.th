# 1. သင့် Project Folder ထဲမှာ index.html ကိုဖန်တီးပါ
echo "<h1>My Data Package</h1>" > index.html

# 2. _redirects ကိုဖန်တီးပါ
echo "/* /index.html 200" > _redirects

# 3. netlify.toml ကိုဖန်တီးပါ
cat > netlify.toml << EOF
[build]
  publish = "."
  command = "echo 'No build required'"
EOF

# 4. netlify.com/drop ကိုသွားပြီး Folder ကိုဆွဲချပါ
